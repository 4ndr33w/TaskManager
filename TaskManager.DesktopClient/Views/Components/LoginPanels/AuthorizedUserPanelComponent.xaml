<UserControl x:Class="TaskManager.DesktopClient.Views.Components.LoginPanels.AuthorizedUserPanelComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskManager.DesktopClient.Views.Components.LoginPanels"
             Visibility="{Binding Path=AuthorizedPanelVisibility}"
             mc:Ignorable="d">
    <StackPanel Orientation="Horizontal" Height="30" Visibility="{Binding AuthorizedPanelVisibility}">
        <Button Content="Change user" 
            Height="20" 
            Width="90" 
            Margin="0,0,5,0" 
            Style="{StaticResource CommonButtonStyle}" 
            Command="{Binding OpenRegisterWindowCommand}"></Button>
        <Button Height="20" Width="200" Margin="5,0,0,0" Style="{StaticResource CommonButtonStyle}">
            <StackPanel Orientation="Horizontal">
                <Label Content="Login as:" Style="{StaticResource CommonLabelStyle}" VerticalAlignment="Top" FontSize="12" Padding="0,0,-6,0"/>
                <Image/>
                <Label Content="{Binding}"/>
            </StackPanel>
        </Button>
        <TextBlock Width="120" Height="20" Margin="5,0,15,0" Style="{StaticResource CommonTextBlockStyle}"></TextBlock>
    </StackPanel>
</UserControl>
